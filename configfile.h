#ifndef CONFIGFILE_H
#define CONFIGFILE_H

/*
#include <QWidget>
#include <QListWidget>
#include "structs_ch.h"
#include "formserverpereferences.h"

namespace Ui {
class ConfigFile;
}

class ConfigFile : public QWidget
{
    Q_OBJECT

public:

     ConfigFile(std::shared_ptr<ConfigInit> inits);
    ~ConfigFile();
     std::shared_ptr<ConfigInit> Inits(){
         return _inits;
     }

private slots:
    void on_pb_save_to_file_clicked(bool checked);

private:
    Ui::ConfigFile *ui;
    std::shared_ptr<ConfigInit> _inits;
};
*/
#endif // CONFIGFILE_H
